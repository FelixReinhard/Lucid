struct String{str}

fn String::get_at(self, index) => __string_get_at(self.str, index);
fn String::concat(self, other) {
  self.str = self.str + other;
} 
// create 
fn String::create() => new String("");
fn String::from(str) => new String(str);
